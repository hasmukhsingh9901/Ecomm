<input
  class="bg-transparent block w-full px-3 py-2 border border-zinc-500 rounded-md mb-3 focus:border-cyan-500 focus:outline-none"
  type="{{type}}"
  placeholder="{{placeholder}}"
  name="{{name}}"
  value="{{value}}"
  autocomplete="{{autocomplete}}"
/>
